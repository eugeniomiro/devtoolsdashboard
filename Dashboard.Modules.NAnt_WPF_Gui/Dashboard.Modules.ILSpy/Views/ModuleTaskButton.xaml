<UserControl x:Class="Techno_Fly.Tools.Dashboard.Modules.ILSpy.Views.ModuleTaskButton"
             x:Name="ModuleTaskButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ribbon="clr-namespace:Microsoft.Windows.Controls.Ribbon;assembly=RibbonControlsLibrary" 
             xmlns:Commands="clr-namespace:Techno_Fly.Tools.Dashboard.Modules.ILSpy.Commands"
             mc:Ignorable="d">
    <ribbon:RibbonButton x:Name="TaskButton" LargeImageSource="..\Ant.ico" Label="NAnt Gui" Command="{Binding ShowModuleView}" />

    <!--<fsc:TaskButton x:Name="TaskButton"
                    Command="{Binding ShowModuleView}" 
                    IsChecked="{Binding IsChecked}"
                    MinWidth="150" 
                    Foreground="Black" 
                    Image="Images/module_a.png" 
                    Text="Module A" 
                    Margin="5,2,5,2" 
                    Background="{Binding Path=Background, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" />-->
</UserControl>
